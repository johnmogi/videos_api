   
        function (data) {
            // names
            const filteredNames = countries.map((country, index) => {
                const {
                    capital
                } = country;
                return {
                    capital
                };
            });

        },

        statusCode: {
            404: function () {
                alert("page not found");
            }
        }